import personajes.*
import config.*
import wollok.game.*
import niveles.*

program PepitaGame {
	game.title("R&M")
	game.height(10)
	game.width(10)
        // game.addVisual(fondo)
	// game.addVisualCharacter(r)
	// game.addVisual(g)
        nivel1.show()
	game.showAttributes(r) //Debug

        config.teclas()

	game.start()
}
